A basic **vite+react+ts** template app that implements CRUD operation of EmployeesNames

commands to run this project once you clone the project and navigate to vite-employee-crud folder
npm install
npm run dev
